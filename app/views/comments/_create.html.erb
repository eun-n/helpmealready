<%= bootstrap_form_for Comment.new, url: post_comments_path(@post, Comment.new), html: { method: :post } do |c| %>
  <%= c.text_area :content %>
  <%= c.hidden_field :post_id, value: @post.id %>
  <%= c.primary %>
<% end %>